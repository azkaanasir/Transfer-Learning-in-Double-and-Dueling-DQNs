grp05_venv/

# analysis working files (regenerable: python analysis/extract_logs.py)
analysis/_logs/
analysis/_out/

# experiment outputs (regenerable: python experiments/sweep.py)
runs/
__pycache__/
*.pyc

# Local working state -- deliberately never pushed.
# This repo is public and the manuscript is under review at ICANN, so the
# reviewer PDFs and the internal audit must not be published from here.
paper/
memory/
